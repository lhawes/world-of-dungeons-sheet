import * as React from 'react';

export interface ItemInstanceCardProps {
  [key: string]: any;
}

export const ItemInstanceCard = ({}: ItemInstanceCardProps) => {
  // connect to the data 

  return (<div>
    ItemInstanceCard
  </div>);
}